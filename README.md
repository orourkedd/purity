React Redux Boilerplate
====================

A very simple react boilerplate.  Runs natively against the node 6 spec (i.e. no transpiling to run in node).

Development
---------------------

Run the following commands:   
`npm run dev` or `npm run dev-front`
`npm run webpack-dev-server`
